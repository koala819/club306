'use client';
// import { createClient } from '../../prismicio';

export default function Handle() {
  // const client = createClient();
  // console.log('check context\n\n', context);
  // if (typeof context.query.article === 'string') {
  //   const articles = await client.getByUID('articles', context.query.article);
  //   console.log('check UID', context.query.article);
  //   console.log('check articles', articles);
  // }
  // const mappedArticles = articles.results.map((article) => ({
  //   uid: article.uid,
  //   data: {
  //     featureImageUrl: article.data.featureImageUrl,
  //     dateEvent: article.data.dateEvent,
  //     title: article.data.title,
  //     description: article.data.description,
  //   },
  // }));

  return <div>Travail pour afficher 1 article du Blog</div>;
}
